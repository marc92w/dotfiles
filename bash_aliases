## get rid of command not found
alias claer='clear'
alias cd..='cd ..'

## get rid of missing permissions for apt-get
alias apt-get='sudo apt-get'

alias ll='ls -la'

## programs
alias diff='colordiff'
alias ping='ping -c 5'

alias mkdir='mkdir -pv' ## create parent directories

